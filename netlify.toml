[build]
  base    = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serves index.html for all routes so hash routing works on reload
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
